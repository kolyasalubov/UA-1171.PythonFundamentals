
def largest_number(first_number, second_number):
    """This function checks the value and 
     prints the one that is the largests"""
    if first_number == second_number:
        return first_number
    elif first_number > second_number:
        return first_number
    else:
        return second_number